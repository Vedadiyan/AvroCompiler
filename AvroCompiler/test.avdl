@register("")
protocol Expedia {
    record Request {
      array<array<array<array<int>>>> test;
      Kind kind;
      Response response;
      Hash hash;
      union {null, Hash} nullableHash;
      union {null, string} fff;
      map<string> str;
      union {map<string>, Kind} str2;
      union {map<string>, Kind, null} str3;
      union {null, map<string>} str4;
    } 
    fixed Hash(16);
    enum Kind {
      OK
    }
    record Response {
      string results;
    }
    Response GetResults(Request request);
}
