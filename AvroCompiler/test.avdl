@register("")
protocol Expedia {
    record Request {
      array<array<array<array<int>>>> test;
      Kind kind;
      Response response;
    }
    enum Kind {
      OK
    }
    record Response {
      string results;
    }
    Response GetResults(Request request);
}
